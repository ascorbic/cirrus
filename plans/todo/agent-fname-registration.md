# Add and Test Fname Registration in Agent FID Creation

## Context

Extracted from `fid-creation-signup.md`. The agent signup flow supports optional
fname registration, but it needs dedicated testing and UX work.

## Requirements

- Fname availability check endpoint/UI
- Fname selection as part of agent signup flow
- Set fname as AT Protocol handle alongside `NNN.fid.is`
- End-to-end test coverage for fname registration path
