{
	"name": "@demo/pds",
	"version": "1.0.0",
	"description": "Demo PDS deployment",
	"type": "module",
	"private": true,
	"dependencies": {
		"@ascorbic/pds-worker": "^0.0.0"
	},
	"devDependencies": {
		"wrangler": "^4.54.0"
	},
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"setup": "pds init --local",
		"setup:prod": "pds init --production",
		"secret:jwt": "pds secret jwt",
		"secret:password": "pds secret password",
		"secret:key": "pds secret key",
		"types": "wrangler types"
	},
	"keywords": [],
	"author": "Matt Kane",
	"license": "MIT"
}
